/*
 * TimerCard Messages
 *
 * This contains all the text for the TimerCard component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.TimerCard";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the TimerCard component!",
  },
});
