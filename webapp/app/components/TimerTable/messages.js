/*
 * TimerTable Messages
 *
 * This contains all the text for the TimerTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TimerTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TimerTable component!',
  },
});
