/*
 * Visual Messages
 *
 * This contains all the text for the Visual container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Visual';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Good luck!',
  },
});
