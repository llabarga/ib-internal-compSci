/*
 * Nav Messages
 *
 * This contains all the text for the Nav container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Nav';

export default defineMessages({
  home: {
    id: `${scope}.home`,
    defaultMessage: 'Home'
  },
});
